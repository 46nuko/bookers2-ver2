<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <table>
        <%= render partial: 'books/list', locals: { users: @users } %>
      </table>
      <h2 class="mt-3">New book</h2>
      <%= render partial: 'books/form', locals: { book: @book } %>
    </div>
    <div class="col-md-8 offset-md-1">
      <h2>Users</h2>
      <table class="table">
        <thead>
        </thead>
        <% @users.each do |user| %>
        <tr>
          <td><% if @user.profile_image.attached? %>
        　       <%= image_tag url_for(@user.profile_image),size:100 %>
              <% else %>
                <%= image_tag 'no_image.jpg',size:"100x100" %>
              <% end %>
          </td>
          <td><%= user.name %></td>
          <td><%= link_to 'Show', user_path(@book) %></td>
        </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>