<h1>Editing book</h1>
<%= form_with model: @book do |f| %>
  <label for="booktitle">Title</label>
  <%= f.text_field :title, autofocus: true, id:"booktitle" %>
  <label for="bookbody">Opinion</label>
  <%= f.text_area :body, id:"bookbody" %>
  <%= f.submit "Update Book" %>
<% end %>